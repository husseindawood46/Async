console.log('Hello world');

const addToNumbers = (a, b) => a + b;

addToNumbers(5, 5);

const subtractToNumbers = (a, b) => a - b;

subtractToNumbers(5, 5);

const mutliplyToNumbers = (a, b) => a * b;

mutliplyToNumbers(5, 5);
